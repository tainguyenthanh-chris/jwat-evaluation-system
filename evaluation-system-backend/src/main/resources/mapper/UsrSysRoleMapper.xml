<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.clt.evaluation_system_backend.mapper.UsrSysRoleMapper">

    <insert id="insertUsrSysRole"> INSERT INTO usr_sys_role ( usr_id, sys_role_id,
        usr_role_status_cd, cre_usr_id, cre_dt, upd_usr_id, upd_dt, del_flg ) VALUES ( #{usrId},
        #{sysRoleId}, #{status}, #{createBy}, NOW(), #{createBy}, NOW(), '0' ) </insert>

    <update id="delete"> UPDATE usr_sys_role SET del_flg = '1', upd_usr_id = #{updateBy}, upd_dt =
        NOW() WHERE usr_id = #{usrId} AND sys_role_id = #{sysRoleId} AND del_flg = 'F' </update>

</mapper>